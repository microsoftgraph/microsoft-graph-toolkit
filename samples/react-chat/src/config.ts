const config = {
  clientId: 'b523855e-a25d-4f35-a130-375f7ba26bb0', //this is the app if of the app registration
  isWebProxyEnabled: true, //this is the web proxy setting to enable the web proxy
  webProxyURL: 'http://localhost:5000/api', //this is the web proxy url
  webProxyAPIScope: 'api://7095e991-0488-48cc-beb4-cf18103f806a/API.All' //this is the web proxy api scope
};

export default config;
