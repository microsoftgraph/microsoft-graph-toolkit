import { mergeStyles } from '@fluentui/react';
const styles = {
  chatHeader: mergeStyles({
    display: 'flex',
    lineHeight: '32px',
    fontSize: '18px',
    fontWeight: 700
  })
};
export { styles };
