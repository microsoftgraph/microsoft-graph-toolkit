import { mergeStyles } from '@fluentui/react';
const styles = {
  chatHeader: mergeStyles({
    display: 'flex',
    alignItems: 'center',
    columnGap: '4px',
    lineHeight: '32px',
    fontSize: '18px',
    fontWeight: 700
  })
};
export { styles };
