import { css, CSSResult } from 'lit';
/**
 * exports lit-element css
 * @export styles
 */
export const styles: CSSResult[] = [
  css`
    @tailwind base;
    @tailwind components;
    @tailwind utilities;
    @tailwind screens;  
  `
];
